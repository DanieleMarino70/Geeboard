import { Placeholder } from "@/components/placeholder";

export default function Page() {
  return (
    <Placeholder
      crumbs={["Ashfold", "Activity"]}
      title="Activity feed"
      artboard="Main.dc.html"
    >
      Every event across the workspace, filterable by actor and server.
    </Placeholder>
  );
}
