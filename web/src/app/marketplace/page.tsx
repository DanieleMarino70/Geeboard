import { Placeholder } from "@/components/placeholder";

export default function Page() {
  return (
    <Placeholder
      crumbs={["Ashfold", "Marketplace"]}
      title="Marketplace"
      artboard="Marketplace.dc.html"
    >
      Version-checked plugins, filtered to what your server build can actually run.
    </Placeholder>
  );
}
