import { Placeholder } from "@/components/placeholder";

export default function Page() {
  return (
    <Placeholder
      crumbs={["Ashfold", "Nodes"]}
      title="Nodes"
      artboard="NodeDetail.dc.html"
    >
      Node health, port allocations and the servers each machine hosts.
    </Placeholder>
  );
}
