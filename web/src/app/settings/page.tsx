import { Placeholder } from "@/components/placeholder";

export default function Page() {
  return (
    <Placeholder
      crumbs={["Ashfold", "Settings"]}
      title="Settings"
      artboard="Settings.dc.html"
    >
      Identity, runtime, behaviour and the danger zone.
    </Placeholder>
  );
}
